import { StubPage } from "@/components/StubPage";

export default function NotificationsPage() {
  return (
    <StubPage
      title="Notifications"
      body="Analysis complete, limit reached, and product updates will appear here. Add toast provider in root layout when ready."
    />
  );
}
