import { StubPage } from "@/components/StubPage";

export default function AdminFlaggedPage() {
  return (
    <StubPage
      title="Flagged responses"
      body="Review dangerous or incorrect model outputs. Queue + actions (mark safe, edit knowledge) — wire when logging pipeline exists."
    />
  );
}
